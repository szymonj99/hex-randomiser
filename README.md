# hex-randomiser

The program will overwrite files in the current directory with random hexadecimal data.

The main idea is to:
1. Ask user to confirm the current directory.
2. Get a list of all the files in the current directory.
3. For each file, overwrite the hex data with random data.